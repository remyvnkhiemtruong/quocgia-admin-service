# Copy nội dung dưới vào file .env tại thư mục admin-service trên server
# (thư mục chứa docker-compose.yml)

PORT=5000
NODE_ENV=production
BASE_URL=https://gddpcamau.io.vn

# Database - phải trùng với postgres container (service name = hostname)
DB_HOST=postgres
DB_PORT=5432
DB_USER=heritage_user
DB_PASSWORD=heritage_pass_123
DB_NAME=heritage_db
